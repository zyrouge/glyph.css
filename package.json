{
  "name": "glyph.css",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "test": "npm run build && node ./scripts/default",
    "demo": "npm run build && node ./scripts/demo"
  },
  "keywords": [],
  "author": "zyrouge",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^15.0.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  }
}